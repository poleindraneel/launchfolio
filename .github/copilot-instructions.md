# Copilot instructions

This project follows **AGENTS.md** (repo root) — read it first.

In short: to change anything on the site, edit only `config/site.config.js`. Do not edit
`js/render.js`, `css/styles.css`, or `index.html` for content or theme. Validate with
`npm run check`. Keep it dependency-free and static.
